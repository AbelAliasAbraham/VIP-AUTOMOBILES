<?php
if(isset($_post['Submit']))
{
	$txtName = $_POST['fname'];
	$txtlname= $_POST['lname'];
	$txtemail = $_POST['email'];
	$txtnumber = $_POST['number'];
	$txtqueries = $_POST['queries'];


}
$servername="localhost";
$username="root";
$pass="";
$dbname="mcontact";
$con = new mysqli($servername,$username,$pass,$dbname);

if($con->connect_error){
	die("Connection Failed:".$con->connect_error);
}

// database insert SQL code
$sql = "INSERT INTO `v_service` (`Id`, `First Name`, `Last Name`, `Email ID`, `Mobile Number`,`Queries`) VALUES ('0', '$txtName', '$txtregistration', '$txtcompany', '$txtmodel','$txtchasis','$txtdate','$txtphone')";

// insert in database 
$rs = mysqli_query($con, $sql);

if($rs)
{
	echo "Contact Records Inserted";
}
mysqli_close($con);

?>
